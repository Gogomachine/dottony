/**
 * Жетоны: откуда берутся.
 *
 * Цены лежат там, где продаётся вещь (наклейки и ячейки — в `marks.ts`,
 * взнос — в `tourney.ts`), а здесь то, чем платят: правило заработка. Оно
 * одно на всю игру, и держать его рядом с ценами обязательно — иначе цена
 * назначается в одном файле, а время, за которое её набирают, меняется в
 * другом, и экономика разъезжается молча.
 *
 * **Заход даёт жетон.** Не победа, а доведённый до конца заход или матч:
 * проигравший потратил то же время. Чаще, чем раз в `TOKEN_GAP_SECONDS`
 * (сервер), жетон не выдаётся — иначе скрипт, шлющий один и тот же журнал
 * по кругу, зарабатывал бы быстрее человека.
 *
 * **Смена даёт двадцать пять.** Первый заход дня — надбавка. Она делает
 * ровно две вещи, и обе нужны:
 *
 * 1. Даёт повод прийти завтра. До неё возвращаться было незачем: дневная
 *    таблица обнулялась сама по себе, и это единственное, что менялось.
 * 2. Делает турнир достижимым. Жетон в минуту — это вечер игры на четверть
 *    взноса; со сменой вечер даёт около полусотни, и вход по карману тому,
 *    кто играет по вечерам, а не только тому, у кого есть свободные сутки.
 *
 * День у смены тот же, что у турнира (московская полночь): две вещи,
 * которые игрок делает раз в день, обязаны начинаться в один и тот же миг.
 */
export const SHIFT_BONUS = 25;
