/**
 * Настройки «ощущения» игры — всё, что влияет на отклик и плавность,
 * собрано в одном месте, чтобы крутить баланс не роясь по коду.
 * Значения подбирались руками на телефоне и мыши.
 */
export const FEEL = {
  /** Радиус захвата точки, в долях клетки: у пальца он должен быть щедрее. */
  hitRadiusMouse: 0.42,
  hitRadiusTouch: 0.6,

  /**
   * Шаг трассировки пути указателя, в долях клетки. Между событиями
   * pointermove палец успевает пройти несколько клеток — проверяем отрезок,
   * иначе на быстром движении цепочка рвётся.
   */
  traceStep: 0.25,

  /** Насколько вырастает точка, попавшая в цепочку. */
  activeScale: 1.22,
  /**
   * Прозрачность точек, не участвующих в цепочке. Приглушая фон, делаем
   * собранную цепочку читаемой с одного взгляда.
   */
  dimAlpha: 0.42,
  /**
   * Линия цепочки: она же провод, по которому идёт ток, поэтому плотная и
   * почти непрозрачная — точки должны читаться как единая цепь, а не как
   * отдельные кружки с перемычками.
   */
  chainAlpha: 0.95,
  chainWidth: 0.3,
  /** Жёсткость и затухание пружины масштаба: чем выше, тем резче «щелчок». */
  scaleStiffness: 420,
  scaleDamping: 22,

  /** Ускорение падения, клеток/с². */
  gravity: 36,
  /** Доля скорости, сохраняемая при отскоке. */
  bounce: 0.12,
  /** Сплющивание при приземлении и скорость его восстановления. */
  squashAmount: 0.32,
  squashRecovery: 9,
  /** Задержка падения по столбцам, сек — точки сыплются волной, а не стеной. */
  columnStagger: 0.018,

  /** Время жизни следа снятой точки и волны сбора, сек. */
  ghostLife: 0.34,
  waveLife: 0.42,

  /** Вибрация при захвате точки, мс: длиннее цепочка — заметнее отклик. */
  hapticBase: 5,
  hapticPerDot: 1.5,
  hapticMax: 18,
} as const;
