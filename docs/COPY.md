# Тексты dotoscope

Все строки, которые видит игрок: подписи, кнопки, сообщения, правила, словарь.
Собрано из `apps/client/index.html`, `apps/client/src/main.ts`,
`apps/client/src/cabinet.ts`, `packages/core/src/rating.ts`,
`apps/server/src/app.ts`, `apps/server/src/ghost.ts`.

**Как править.** Меняй текст прямо в этом файле — в колонке «Текст». Я перенесу
правки в код по колонке «Где в коде». В фигурных скобках — подстановки: `{счёт}`,
`{имя}`, менять их нельзя, а переставлять местами можно.

---

## 1. Имя, шильдик, вкладка

| Текст | Где показывается | Где в коде |
|---|---|---|
| `dotoscope — прибор для наблюдения за круглыми объектами` | Заголовок вкладки браузера | `index.html`, `<title>` |
| `Round things inc.` | Шильдик, первая строка | `index.html`, `.plate .id` |
| `Dotoscope · model 36` | Шильдик, вторая строка | `index.html`, `.plate .id` |
| `4 сигнала · 36 точек` | Подвал, правый верх | `index.html`, `.brand` |
| `образец #{сид}` | Подвал, под ним | `main.ts`, `seedEl` |
| `образец —` | То же до первой партии | `index.html`, `#seed` |
| `Метки цвета` | Подсказка кнопки ◍ | `index.html`, `#mark-toggle` |
| `Свет` | Подсказка кнопки ◐ | `index.html`, `#theme-toggle` |

## 2. Приборная строка

| Текст | Где показывается | Где в коде |
|---|---|---|
| `Отсчёты` | Подпись слева (счёт партии) | `index.html`, `.strip` |
| `Остаток` | Подпись справа (время) | `index.html` / `main.ts`, `timeLabelEl` |
| `Комбо` | Подпись справа в бесконечном режиме | `main.ts`, `updateHud` |
| `Соперник` | Подпись среднего поля в дуэли | `index.html`, `#vs-name` |
| `×{увеличение}` | Множитель рядом со счётом | `main.ts`, `updateStreak` |
| `∞` | Значение справа, если челленджа нет | `main.ts`, `updateHud` |

## 3. Служебный экранчик (резонанс)

| Текст | Когда | Где в коде |
|---|---|---|
| `Резонанс · ожидание` | Партия ещё не начата | `main.ts`, `updateMini` |
| `Резонанс · сигнал ровный` | Партия идёт, резонанса нет | `main.ts`, `updateMini` |
| `Резонанс · настройка` | За 5 секунд до резонанса | `main.ts`, `updateMini` |
| `Резонанс · ×{множитель}` | Резонанс идёт | `main.ts`, `updateMini` |

## 4. Строка состояния в окуляре

| Текст | Когда | Где в коде |
|---|---|---|
| `Готов к наблюдению` | Новый образец, часы не пущены | `main.ts`, `startGame` |
| `Наблюдение идёт` | После закрытия панели | `main.ts`, `closeMenu` |
| `Снято {точек} · +{отсчёты} ×{множитель}` | После хода | `main.ts`, обработчик хода |
| `Комбо {число} — рекорд захода` | Новый рекорд комбо | `main.ts`, `countCombo` |
| `Цепь {длина} — следующий ход идёт в это же комбо` | Цепочка взяла порог продления | `main.ts`, `countCombo` |
| `Комбо {число} · {место}-е место` | Сервер подтвердил рекорд | `main.ts`, `sendCombo` |
| `Журнал захода полон — новый образец для нового рекорда` | Исчерпан журнал бесконечного захода | `main.ts`, `countCombo` |
| `Панель управления` | Панель открыта | `main.ts`, `openMenu` |
| `Наблюдение завершено` | Партия кончилась | `main.ts`, `showResult` |
| `Цепь {N}` | Счётчик длины цепочки, правый нижний угол | `main.ts`, `chainCountEl` |

Стартовые значения в разметке, до первого кадра: `Сигнал ровный` (строка
состояния) и `Цепь 0` (счётчик) — `index.html`, `#stat` и `#chain-count`.
| `+{отсчёты} ×{множитель}` | Всплывающая цифра над ходом | `main.ts`, `showFloatingPoints` |

## 5. Клавиши

| Текст | Подпись под ним | Где в коде |
|---|---|---|
| `Профиль` | `Рекорды, друзья` | `index.html`, `#key-profile` |
| `Меню` | `Режимы, правила` | `index.html`, `#key-menu` |
| `Наблюдать` | `Новый образец` | `index.html`, `#key-go` |
| `Сброс` | (та же клавиша во время партии) | `main.ts`, `updateGoKey` |
| `Повторить` | (та же клавиша после конца партии) | `main.ts`, `updateGoKey` |

## 6. Панель управления (меню)

| Текст | Тип | Где в коде |
|---|---|---|
| `Панель управления` | Заголовок | `index.html`, `.mtitle` |
| `Наблюдение` | Раздел | `index.html`, `#menu-list` |
| `Спринт` | Пункт | `index.html`, `#menu-list` |
| `Бесконечный` | Пункт | `index.html`, `#menu-list` |
| `Дуэль` | Пункт | `index.html`, `#menu-list` |
| `Прибор` | Раздел | `index.html`, `#menu-list` |
| `Профиль` | Пункт | `index.html`, `#menu-list` |
| `Друзья` | Пункт | `index.html`, `#menu-list` |
| `Правила` | Пункт | `index.html`, `#menu-list` |

## 7. Итог партии в окуляре

| Текст | Когда | Где в коде |
|---|---|---|
| `Наблюдение завершено` | Заголовок итога | `main.ts`, `showResult` |
| `{счёт}` | Крупное число | `main.ts`, `resultBigEl` |
| `Рекорд · {место}-е место` | Спринт побил личный рекорд | `main.ts`, `finishSprint` |
| `{место}-е место · рекорд {число}` | Спринт слабее рекорда | `main.ts`, `finishSprint` |
| `образец #{сид}` | Подпись под числом в остальных режимах | `main.ts`, `showResult` |

## 8. Дуэль

### Лист выбора

| Текст | Где в коде |
|---|---|
| `Дуэль` (заголовок) | `index.html`, `#duel-sheet` |
| `Быстрый матч` | `index.html`, `#duel-quick` |
| `Позвать друга` | `index.html`, `#invite` |
| `Ввести код` | `index.html`, `#join-code` |
| `Назад` | `index.html`, `#duel-cancel` |

### Сообщения матча

| Заголовок | Текст | Когда |
|---|---|---|
| `Дуэль` | `Подключаюсь — бесплатный сервер просыпается до минуты…` | Соединение |
| `Дуэль` | `Ищем живого соперника…` | Поиск |
| `Дуэль` | `Никого рядом — подбираю запись чужого матча…` | Поиск затянулся |
| `Ждём друга` | `Продиктуй другу код комнаты. Он вводит его кнопкой «Ввести код» — и матч начнётся сам.` | Комната с другом |
| `Дуэль` | `Время вышло, считаем результат…` | Конец матча |
| `Связь пропала` | `Возвращаю в матч… Время идёт.` | Обрыв связи |
| `Дуэль` | `Ход не принят сервером. Матч прерван.` | Рассинхрон |
| `Дуэль` | `Не удалось войти. Попробуй ещё раз.` | Ошибка входа |
| `Дуэль` | `Нужен вход. Сыграй спринт — он создаст профиль.` | Нет профиля |
| `Дуэль` | `Связь с сервером потеряна.` | Сервер недоступен |
| `Дуэль` | `Дуэли доступны только с сервером.` | Офлайн-режим |
| `Дуэль` | `Код слишком короткий — проверь и введи ещё раз.` | Плохой код комнаты |
| `Победа!` / `Поражение` / `Ничья` | заголовок итога матча | Конец матча |
| `Код комнаты` | подпись над кодом | Комната с другом |
| `Позвал в Telegram — ждём друга.` | | Приглашение ушло |
| `В Telegram позвать не вышло — продиктуй другу код.` | | Приглашение не ушло |
| `+ {имя} в друзья` | кнопка на экране результата | После матча |
| `{имя} в друзьях ✓` | она же после нажатия | |
| `Не удалось добавить` | она же при ошибке | |

Все места в коде — `main.ts`, `startDuel` / `handleDuelMessage` / `showOverModal`.

### Кнопки окна

| Текст | Когда | Где в коде |
|---|---|---|
| `Понятно` | Обычный итог | `main.ts`, `showOverModal` |
| `Закрыть` | Просмотр таблицы поверх партии | `main.ts`, `showOverModal` |
| `Отменить` | Идёт поиск соперника | `main.ts`, `showOverModal` |

## 9. Таблицы

| Заголовок | Пустая таблица | Своя строка | Ошибка |
|---|---|---|---|
| `Рейтинг` | `Таблица пока пуста: в неё попадают те, кто прошёл калибровку в дуэлях.` | `Ты на {место}-м месте · {лига}` / `Пройди калибровку в дуэлях, чтобы попасть в таблицу.` | `Таблица рейтинга недоступна. Попробуй позже.` |
| `Рекорды спринта` | `Таблица пока пуста. Три минуты на максимум отсчётов — и ты в ней.` | `Твой рекорд {число} · {место}-е место` / `Сыграй спринт, чтобы попасть в таблицу.` | `Таблица спринта недоступна. Попробуй позже.` |
| `Максимальное комбо` | `Таблица пока пуста. Считаются отсчёты за один ход в бесконечном режиме.` | `Твой рекорд {число} · {место}-е место` / `Сделай дорогой ход в бесконечном режиме, чтобы попасть в таблицу.` | `Таблица комбо недоступна. Попробуй позже.` |

Плюс общее `Загружаю таблицу…`. Место в коде — `main.ts`, `showRatingBoard` /
`showSprintBoard` / `showComboBoard`.

## 10. Кабинет

| Текст | Где показывается | Где в коде |
|---|---|---|
| `вход: {способы}` | Под именем | `cabinet.ts`, `renderProfile` |
| `гость` / `Telegram` / `кошелёк TON` | Названия способов входа | `cabinet.ts`, `LOGIN_NAMES` |
| `Сменить имя` | Подсказка кнопки ✎ | `index.html`, `#cab-rename` |
| `Привязать Telegram` | Кнопка | `index.html` / `cabinet.ts` |
| `Подтверди в Telegram…` | Она же после нажатия | `cabinet.ts`, `linkTelegram` |
| `Пока недоступно` | Она же, если бот не настроен | `cabinet.ts`, `linkTelegram` |
| `Имя не сохранилось — попробуй ещё раз` | Ошибка переименования | `cabinet.ts`, `rename` |
| `Калибровка` | Вместо лиги до пяти матчей | `cabinet.ts`, `renderProfile` |
| `{сыграно} из {нужно} рейтинговых дуэлей — потом лига` | Под ней | `cabinet.ts`, `renderProfile` |
| `до лиги «{лига}» ещё {очков}` | Прогресс лиги | `cabinet.ts`, `renderProfile` |
| `высшая лига` | На вершине | `cabinet.ts`, `renderProfile` |
| `Наработка` | Заголовок счётчика | `index.html`, `.tot-label` |
| `до {отметка} — ещё {осталось}` | Путь до следующей отметки | `cabinet.ts`, `renderTotal` |
| `все отметки пройдены` | Все отметки взяты | `cabinet.ts`, `renderTotal` |
| `рейтинг` / `место` / `дуэли` / `спринт` / `комбо` | Подписи показателей | `index.html`, `.cab-stats` |
| `спринт · #{место}` / `комбо · #{место}` | Они же с местом | `cabinet.ts`, `renderProfile` |
| `Таблица рейтинга` / `Рекорды спринта` / `Таблица комбо` | Кнопки таблиц | `index.html`, `.cab-actions` |
| `Матчи` / `Друзья` | Вкладки | `index.html`, `.cab-tabs` |
| `Загружаю…` | Список грузится | `cabinet.ts`, `show` |
| `Профиль недоступен — сервер не ответил.` | Сервер молчит | `cabinet.ts`, `show` |
| `Матчей ещё не было. Сыграй дуэль!` | Пустая история | `cabinet.ts`, `renderHistory` |
| `Посмотреть партию` | Подсказка строки истории | `cabinet.ts`, `renderHistory` |
| `Твой код` | Подпись кода друга | `index.html`, `.code-label` |
| `······` | Заглушка кода, пока он не загружен | `index.html`, `#cab-code` |
| `Добавить` | Кнопка добавления по коду | `index.html`, `#cab-add-friend` |
| `Друзей пока нет. Дай свой код или добавь по чужому.` | Пустой список | `cabinet.ts`, `renderFriends` |
| `Список друзей недоступен.` | Ошибка загрузки | `cabinet.ts`, `loadFriends` |
| `Такого кода нет — проверь и попробуй ещё раз.` | Плохой код друга | `cabinet.ts`, `addByCode` |
| `Позвать` | Кнопка у друга | `cabinet.ts`, `renderFriends` |
| `калибровка` | Вместо лиги друга | `cabinet.ts`, `renderFriends` |
| `Недавние соперники` | Заголовок раздела | `index.html`, `#recent-title` |
| `+ в друзья` | Кнопка у недавнего соперника | `cabinet.ts`, `renderRecent` |

## 11. Реплей

| Текст | Когда | Где в коде |
|---|---|---|
| `Реплей` | Заголовок и полоса | `main.ts`, `startReplay` |
| `Реплей · {соперник}` | Полоса с именем | `main.ts`, `startReplay` |
| `Загружаю запись…` | Загрузка | `main.ts`, `startReplay` |
| `Записи этой партии нет.` | Записи нет | `main.ts`, `startReplay` |
| `Реплей окончен` | Конец прокрутки | `main.ts`, `startReplay` |
| `Соперник: {имя}` | Под ним | `main.ts`, `startReplay` |
| `Стоп` | Кнопка на полосе | `index.html`, `#replay-stop` |

## 12. Правила (полный текст)

Заголовок окна: **Правила**, кнопка внизу: **Понятно**.

### Наблюдение
> Ведите палец по соседним точкам одного цвета — по горизонтали, вертикали и
> диагонали. Цепочка засчитывается от **трёх** точек.
>
> Каждая следующая точка в цепочке дороже предыдущей, поэтому одна длинная
> цепочка ценнее двух коротких.

### Линзы
> Цепочка от **десяти** точек шлифует **линзу** — она помечена кольцом с ядром.
> Соберите её следующей цепочкой: вспышка убирает соседние точки, а отсчёты хода
> умножаются.
>
> Линзы подряд повышают **увеличение**: ×2, ×3, ×4 и дальше. Цепочка без линзы
> сбрасывает его на единицу.

### Резонанс
> Раз в 45 секунд прибор на 8 секунд входит в резонанс с одним цветом — его
> цепочки дают ×2. Множители перемножаются: увеличение ×3 в резонансе даёт ×6.

### Режимы
> **Спринт** — три минуты на максимум отсчётов. Попыток сколько угодно, в общую
> таблицу идёт лучшая; счёт подтверждает сервер, переигрывая заход. Стоп-кадра
> здесь нет: остановка часов дала бы фору.
>
> **Бесконечный** — без таймера. Отсчёты копятся в наработку прибора, а место
> таймера занимает челлендж: **максимальное комбо** — лучшие отсчёты за один ход.
> Длинная цепочка, линзы под ней и резонанс множатся в одном ходу — это и есть
> рекорд. Цепочка от **22** точек продлевает комбо на один следующий ход: его
> отсчёты складываются с этими. По комбо есть общая таблица, и рекорд
> подтверждает сервер, переигрывая заход.
>
> **Дуэль** — 90 секунд на одинаковом образце с живым соперником. Если рядом
> никого, соперника отыгрывает запись чужой партии — она честно помечена.
> Рейтинг меняется только в быстром матче.

Место в коде: `index.html`, `#rules-sheet`.

## 13. Лиги

Порядок и пороги рейтинга — `packages/core/src/rating.ts`.

| Лига | С рейтинга | Ключ в коде |
|---|---|---|
| `Лупа` | 0 (старт — 1500) | `loupe` |
| `Бинокль` | 1600 | `binocular` |
| `Микроскоп` | 1750 | `microscope` |
| `Телескоп` | 1900 | `telescope` |
| `Обсерватория` | 2100 | `observatory` |

## 14. Соперники

| Текст | Что это | Где в коде |
|---|---|---|
| `Эталон` | Синтетический соперник, когда записей ещё нет | `ghost.ts`, `SYNTHETIC_NAME` |
| `{имя} · запись` | Пометка призрака — записи чужой партии | `main.ts`, `cabinet.ts` |
| `Соперник` | Заглушка, если имя неизвестно | `main.ts`, `cabinet.ts` |
| `Игрок` | Имя гостя по умолчанию | `main.ts`, `guestName` |

## 15. Диалоги (системные окна браузера)

| Текст | Когда | Где в коде |
|---|---|---|
| `Имя для таблиц:` | Первый выход на сервер | `main.ts`, `guestName` |
| `Код комнаты от друга:` | Вход в комнату | `main.ts`, `#join-code` |
| `Код друга:` | Добавление друга | `cabinet.ts`, `addByCode` |
| `Новое имя:` | Переименование | `cabinet.ts`, `rename` |
| `Удалить {имя} из друзей?` | Удаление друга | `cabinet.ts`, `renderFriends` |

## 16. Telegram-бот

| Текст | Когда | Где в коде |
|---|---|---|
| `dotoscope — прибор для наблюдения за круглыми объектами.`<br>`Соединяй точки, шлифуй линзы, вызывай друзей на дуэль.`<br><br>`ROUND THINGS INC` | Ответ на `/start` | `app.ts`, `handleStart` |
| `🔭 Открыть прибор` | Кнопка под сообщением | `app.ts` |
| `Готово: теперь этот Telegram открывает твой профиль на любом устройстве.` | Привязка удалась | `app.ts`, `handleStart` |
| `Этот аккаунт уже привязан.` | Повторная привязка | `app.ts`, `handleStart` |
| `Этот Telegram уже привязан к другому профилю. Отвяжи его там или входи под ним.` | Занятый Telegram | `app.ts`, `handleStart` |
| `Код привязки не подошёл — он живёт десять минут. Возьми новый в профиле.` | Протухший код | `app.ts`, `handleStart` |
| `Такого кода друга нет — проверь ссылку.` | Плохая ссылка друга | `app.ts`, `handleStart` |
| `Это твой собственный код.` | Свой же код | `app.ts`, `handleStart` |
| `{имя} теперь у тебя в друзьях. Зови на дуэль!` | Друг добавлен | `app.ts`, `handleStart` |
| `{имя} зовёт тебя к прибору — дуэль в dotoscope.`<br>`Код комнаты: {код}`<br>`Открой игру → Дуэль → Ввести код.` | Приглашение в комнату | `app.ts`, `/api/friends/:code/invite` |

## 17. Словарь

Слова, на которых держится язык прибора. Меняя одно, стоит пройтись по всем
местам из этой таблицы — иначе интерфейс начнёт говорить на двух языках.

| Слово | Что означает | Где встречается |
|---|---|---|
| `отсчёты` | Очки | Приборная строка, правила, наработка, таблицы |
| `наблюдение` | Игра, партия | Строка состояния, итог, меню |
| `образец` | Расклад поля (сид) | Подвал, клавиша «Наблюдать», дуэль |
| `цепочка` / `цепь` | Соединённые точки | Правила, счётчик в окуляре |
| `линза` | Заряд от длинной цепочки | Правила, поле |
| `увеличение` | Множитель за серию линз | Приборная строка, правила |
| `резонанс` | Фаза удвоения цвета | Экранчик, правила |
| `комбо` | Отсчёты за один ход | Приборная строка, таблица, правила |
| `наработка` | Сумма отсчётов за всё время | Кабинет |
| `стоп-кадр` | Заморозка партии под панелью | Правила, концепция |
| `калибровка` | Первые пять рейтинговых дуэлей | Кабинет, таблица рейтинга |
| `запись` | Призрак — сохранённая чужая партия | Дуэль, история |
| `Эталон` | Синтетический соперник | Дуэль |
| `прибор` | Сама игра | Бот, меню, кабинет |
