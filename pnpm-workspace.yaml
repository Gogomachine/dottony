packages:
  - packages/*
  - apps/*

# Скрипты установки выполняет только esbuild — ему нужен нативный бинарник.
# С pnpm 10.33 эта настройка переехала сюда из package.json, и там она
# молча игнорируется; без неё сборка сервера осталась бы без бандлера.
onlyBuiltDependencies:
  - esbuild
